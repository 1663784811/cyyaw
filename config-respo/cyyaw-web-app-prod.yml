spring:
  application:
    name: cyyaw-web-app

server:
  port: 7032

eureka:
  instance:
    hostname: 192.168.1.130
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:9090/eureka/